#include <stdio.h>
/**
 * main - this function simply displays a text using printf function
 * Return: 0 after the code excute
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
