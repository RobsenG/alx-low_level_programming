#include <stdio.h>
/**
 * main - This function that print all single digit number with base 10
 *
 * Return: 0 Every time the code run(Excute)
 */
int main (void)
{
	int i;
	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return(0);

}
